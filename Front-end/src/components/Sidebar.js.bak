import React from 'react';
import styled from 'styled-components';

const SidebarContainer = styled.div`
  width: 250px;
  background: #111;
  color: white;
  display: flex;
  flex-direction: column;
  padding: 20px;
`;

const SidebarItem = styled.div`
  margin-bottom: 20px;
  cursor: pointer;

  &:hover {
    background: #333;
  }
`;

const Sidebar = ({ onEnviosClick, onVuelosClick, onAeropuertosClick, onReportesClick, onSimulacionClick }) => {
  return (
    <SidebarContainer>
      <SidebarItem onClick={onEnviosClick}>Envios</SidebarItem>
      <SidebarItem onClick={onVuelosClick}>Vuelos</SidebarItem>
      <SidebarItem onClick={onAeropuertosClick}>Aeropuertos</SidebarItem>
      <SidebarItem onClick={onReportesClick}>Reportes</SidebarItem>
      <SidebarItem onClick={onSimulacionClick}>Simulación</SidebarItem>
    </SidebarContainer>
  );
};

export default Sidebar;
